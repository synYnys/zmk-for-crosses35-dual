include:
  - board: nice_nano_v2
    shield: crosses_left
    artifact-name: crosses_36_left
    # 【ここが重要】警告を無視する命令をビルドの最優先事項として追加します
    cmake-args: -DCONFIG_KCONFIG_ERROR_ON_WARNING=n
  - board: nice_nano_v2
    shield: crosses_right
    artifact-name: crosses_36_right
    snippet: studio-rpc-usb-uart
    # 右側にも同じ命令を追加します
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_KCONFIG_ERROR_ON_WARNING=n
