# 画面は物理的に存在しないので完全にオフ
CONFIG_ZMK_DISPLAY=n

# 【真の最終特効薬】
# 無線時も、有線（USB）と100%完全に同じ「フルパワーで起き続ける状態」を強制維持します。
# 最新システムの省電力バグ（自爆タイミング）を力技で完全に踏み潰して無効化します！
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_IDLE_TIMEOUT_MS=3600000
