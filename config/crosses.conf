# 画面はオフ
CONFIG_ZMK_DISPLAY=n

# 同時接続数とペアリング保持数を明示的に確保（Mac + 左子機 = 計2枠）
CONFIG_BT_MAX_CONN=3
CONFIG_BT_MAX_PAIRED=3

# 【最重要】左右高速タイピング時にバッファが溢れてMacが切れるのを防ぐためのバッファ大幅拡張
CONFIG_BT_BUF_ACL_TX_COUNT=7
CONFIG_BT_L2CAP_TX_BUF_NUM=7
