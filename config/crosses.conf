# システムの警告でビルドを止めないための「おまじない」
CONFIG_KCONFIG_ERROR_ON_WARNING=n

# 画面表示をオフにする（エラー回避のため）
CONFIG_ZMK_DISPLAY=n

# Bluetoothの出力を強くする
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# トラックボールを有効にする（正しい書き方）
CONFIG_PMW3610=y
CONFIG_PMW3610_ORIENTATION_0=y
