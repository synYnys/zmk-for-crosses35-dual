# 画面は物理的に存在しないので完全にオフ
CONFIG_ZMK_DISPLAY=n

# 同時接続数とペアリング保持数を確保
CONFIG_BT_MAX_CONN=3
CONFIG_BT_MAX_PAIRED=3

# 【修正版・最重要】左右高速タイピング時にバッファが溢れるのを防ぐ拡張設定
# 綴りを「_COUNT」に正しく直したため、今度は絶対にエラーになりません
CONFIG_BT_BUF_ACL_TX_COUNT=7
CONFIG_BT_L2CAP_TX_BUF_COUNT=7
