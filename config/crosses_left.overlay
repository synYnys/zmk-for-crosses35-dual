/* config/crosses_left.overlay */

&trackball {
    /* --- スピード設定 --- */
    /* 数字を小さくすると遅く、大きくすると速くなります */
    cpi = <300>;

    /* --- 方向の設定（逆回転） --- */
    
    /* 上下を逆にするならこの行を残す */
    invert-y;

    /* 左右も逆にするなら、下の行の「//」を消して有効にする */
    // invert-x;
};
