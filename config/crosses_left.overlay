/* config/crosses_left.overlay */

&spi1 {
    trackball_peripheral@0 {
        /* スクロールのスピード調整 */
        cpi = <100>;

        /* 回転方向の反転（縦） */
        /* ここを修正しました */
        y-invert;
        
        /* 左右も逆にしたい場合は下の「//」を消して有効にする */
        // x-invert;
    };
};
