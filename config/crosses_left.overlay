/* config/crosses_left.overlay */

&trackball {
    /* スクロールのスピード調整 */
    /* 数字を小さくすると遅く、大きくすると速くなります（標準は多分 600-800くらい） */
    cpi = <300>;

    /* 回転方向の反転 */
    /* 以下の行を入れると方向が逆になります。不要なら消してください */
    invert-y;
};
