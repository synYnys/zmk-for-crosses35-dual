/* config/crosses_left.overlay */

&spi1 {
    trackball_peripheral@0 {
        /* 限界まで遅くする */
        cpi = <10>;

        /* 上下を逆にする（これは効いているのでそのまま） */
        y-invert;
        
        /* 元々入っていた「左右反転」を削除して、動きを逆にする */
        /delete-property/ x-invert;
    };
};
