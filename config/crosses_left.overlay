/* config/crosses_left.overlay */

&spi1 {
    trackball_peripheral@0 {
        /* 確実に効く範囲で遅くする（200 CPI） */
        cpi = <200>;
        
        /* 縦と横を反転（この書き方なら古いZMKでもOK） */
        y-invert;
        x-invert;
    };
};
