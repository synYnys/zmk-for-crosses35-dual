/* config/crosses_right.overlay */

&spi1 {
    trackball_central@0 {
        /* スピード調整: 右側を速くしたいなら数字を大きく */
        cpi = <1200>;

        /* 右側の回転方向設定 */
        /* 必要に応じて「//」を消して有効にしてください */
        // y-invert;
        // x-invert;
    };
};
